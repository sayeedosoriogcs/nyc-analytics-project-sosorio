name: 'nyc_analytics'
version: '1.0.0'
config-version: 2
# Some default settings for dbt
profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
 - "target"
 - "dbt_packages"

# Set up models, which are basically SQL files, to create tables
models:
 nyc_analytics:
   +materialized: table

   assignment_work:
     staging:
       +materialized: table
       +schema: motorvehicle_collisions_crashes_staging

     marts:
       +materialized: table
       +schema: motorvehicle_collisions_crashes_marts
      
       shared:
         +materialized: table

       open_vehicle_apps:
         +materialized: table

       nyc_311:
         +materialized: table